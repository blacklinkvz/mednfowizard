<main>
  <div class="album py-5 bg-body-tertiary">
    <div class="container">
      <div class="container marketing">
        <div class="d-flex justify-content-center"></h1></div>
        <div class="d-flex justify-content-end">
          <% if current_user %>  
            <%= link_to "Nuevo destino", new_destiny_path, :class => "btn btn-sm btn-success rounded-pill px-2" %>
          <% end %>
        </div>
        <%= render "singledestiny", destiny: @destiny %>
        
</div>