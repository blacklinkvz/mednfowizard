        <tr class="table-light">
          <td><%= medicament.id %></td>
          <td><%= medicament.name %></td>
          <td><%= medicament.compound %></td>      
          <td><%= medicament.unit.name %></td>
          <td>
            <div class="btn-group">
             <%= link_to raw("<i class=\"bi bi-pencil-square\"></i>"),  edit_medicament_path(medicament), :class => "btn btn-sm btn-primary rounded-pill mr-1" %>
            </div>
          </td>
        </tr>
      </tbody>
 </div>
</main>