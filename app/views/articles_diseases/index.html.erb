<div class="container py-3">
  <main>
    <%= link_to "Agregar padecimiento", new_articles_disease_path, :class => "btn btn-sm btn-primary rounded-pill px-2" %>
     <table class="table table-primary mt-2">
      <thead>
        <tr>
          <th scope="col">ICD-10 code</th>
          <th scope="col">Padecimiento</th>
          <th scope="col">Especificación</th>
          <th scope="col">Artículo</th>
          <th scope="col">Opciones</th>
        </tr>
      </thead>
      <tbody>
  <% @articles_diseases.sort_by { |articles_disease| articles_disease.article.title }.each do |articles_disease| %>
    <%= render articles_disease %>
          <% end %>
    </div>
  </footer>
