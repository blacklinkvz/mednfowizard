<tr class="table-light">
         
  <td><%= articles_medicament.medicament.name %></td>
  <td><%= articles_medicament.medicament.compound %></td>
  <td><%= articles_medicament.medicament.unit.name %></td>      
  <td><%= articles_medicament.quantity %></td>
  <td><%= articles_medicament.article.title %></td>
  <td>
    <div class="btn-group">
     <%= link_to raw("<i class=\"bi bi-pencil-square\"></i>"),  edit_articles_disease_path(articles_medicament), :class => "btn btn-sm btn-primary rounded-pill mr-1" %>
     <%= button_to raw("<i class=\"bi bi-trash\"></i>"), articles_medicament, method: :delete, :class => "btn btn-sm btn-danger rounded-pill" %>
    </div>
  </td>
</tr>
</tbody>
</div>
</main>
